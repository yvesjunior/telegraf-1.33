import http from 'k6/http';
import { sleep } from 'k6';

export const options = {
  vus: 1,
  duration: '10s',
  ext: {
    output: [
      {
        // Use the remotewrite output
        output: 'xk6-output-remotewrite',
        url: 'https://<your-mimir-endpoint>/api/v1/push',
        headers: {
          'X-Scope-OrgID': '<your-tenant-id>', // if required by your Mimir setup
        },
        // Optional: basic auth, TLS, etc.
        // username: 'user',
        // password: 'pass',
        // tlsSkipVerify: true,
      },
    ],
  },
};

export default function () {
  http.get('https://test.k6.io');
  sleep(1);
}
